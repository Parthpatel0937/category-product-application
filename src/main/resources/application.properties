#   H2 Database
#Telling springboot that we need H2 Console Enabled
#spring.h2.console.enabled=true
#URL of H2 database
#spring.datasource.url=jdbc:h2:mem:test
#Indicate that Sql statements generated by hibernate should be logged into the console(Tell springboot to print all SQL queries)
#spring.jpa.show-sql=true
#Telling springboot to recreate the table everytime the application is runned & droped when stopped
#spring.jpa.hibernate.ddl-auto=update
# Springboot uses hibernate to map or create tables
d

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/productdb
pring.datasource.username=parth
spring.datasource.password=changemeinprod!
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
